{
  "name": "@clickup-openclaw/clickup-client",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@clickup-openclaw/shared": "file:../shared"
  },
  "devDependencies": {
    "@types/node": "^22.13.1",
    "typescript": "^5.6.3"
  }
}
