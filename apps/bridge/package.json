{
  "name": "@clickup-openclaw/bridge",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "node --env-file-if-exists=../../.env ./scripts/dev.mjs",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@clickup-openclaw/clickup-client": "file:../../packages/clickup-client",
    "@clickup-openclaw/observability": "file:../../packages/observability",
    "@clickup-openclaw/shared": "file:../../packages/shared",
    "@clickup-openclaw/state": "file:../../packages/state",
    "fastify": "^5.2.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "tsx": "^4.19.1",
    "typescript": "^5.6.3"
  }
}
