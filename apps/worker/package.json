{
  "name": "@clickup-openclaw/worker",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@clickup-openclaw/shared": "file:../../packages/shared",
    "@clickup-openclaw/state": "file:../../packages/state",
    "@clickup-openclaw/workboard": "file:../../packages/workboard"
  },
  "devDependencies": {
    "tsx": "^4.19.1",
    "typescript": "^5.6.3"
  }
}
