{
  "name": "clickup-openclaw",
  "private": true,
  "version": "0.1.0",
  "packageManager": "npm@10.8.2",
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspace @clickup-openclaw/bridge",
    "lint": "npm run lint --workspaces",
    "typecheck": "npm run typecheck --workspaces"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^22.13.1",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "fastify": "^5.2.1",
    "zod": "^3.24.1"
  }
}
